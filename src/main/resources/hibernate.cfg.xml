<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/hospital_tramuntana</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">postgres</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Names the annotated entity class -->
        <mapping class="com.example.models.Paciente"/>
        <mapping class="com.example.models.Medico"/>
        <mapping class="com.example.models.HistorialMedico"/>
        <mapping class="com.example.models.Cita"/>
        <mapping class="com.example.models.Resultado"/>
        <mapping class="com.example.models.Mensaje"/>
    </session-factory>
</hibernate-configuration>